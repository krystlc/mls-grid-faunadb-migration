{
  "name": "mls-grid",
  "version": "1.0.0",
  "description": "MLS Grid Database Replication",
  "main": "index.js",
  "scripts": {
    "build": "babel src/index.js --watch -d dist",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alex.valle@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.0",
    "@babel/node": "^7.8.7",
    "@babel/preset-env": "^7.9.0"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "dotenv": "^8.2.0"
  }
}
